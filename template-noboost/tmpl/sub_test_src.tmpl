#include "gtest/gtest.h"
#include "{MODNAME_LOWER_ABBREV}{SUBMODNAME_LOWER}_mock.h"

TEST_F({MODNAME_CAP_ABBREV}{SUBMODNAME_CAP}Test, XXX)
{{
	EXPECT_EQ(0, 0);
}}

#ifdef ENABLE_GMOCK

using ::testing::Return;
using ::testing::InSequence;

TEST_F({MODNAME_CAP_ABBREV}{SUBMODNAME_CAP}MockTest, XXX)
{{
	EXPECT_EQ(0, 0);
}}

#endif
